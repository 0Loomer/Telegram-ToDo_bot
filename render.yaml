services:
  - type: worker
    name: telegram-todo-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    plan: free
    autoDeploy: true

envVars:
  - key: PYTHON_VERSION
    value: 3.13.0
